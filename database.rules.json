{
  "rules": {
    "chatTyping": {
      "$chatId": {
        "user": {
          ".write": "true",
          ".read": "true"
        },
        "admin": {
          ".write": "auth != null && (auth.token.role == 'ADMIN' || auth.token.role == 'SUPER_ADMIN')",
          ".read": "true"
        }
      }
    },
    "presence": {
      "$presenceId": {
        ".write": "true",
        ".read": "true"
      }
    }
  }
}
