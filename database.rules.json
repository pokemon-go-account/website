{
  "rules": {
    "chatTyping": {
      "$chatId": {
        ".read": "true",
        ".write": "true",
        ".validate": "newData.hasChildren()"
      }
    },
    "presence": {
      ".read": "true",
      "$presenceKey": {
        ".write": "true",
        ".validate": "newData.hasChildren()"
      }
    },
    "analytics": {
      ".read": "true",
      ".write": "true",
      ".validate": "newData.hasChildren()"
    }
  }
}
